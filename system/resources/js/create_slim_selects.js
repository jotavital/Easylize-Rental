var placeholder = "Selecione";

new SlimSelect({
    select: '#marcaSelect',
    placeholder: placeholder,
    allowDeselect: true
});

new SlimSelect({
    select: '#modeloSelect',
    placeholder: placeholder,
    allowDeselect: true
});

new SlimSelect({
    select: '#categoriaVeiculoSelect',
    placeholder: placeholder,
    allowDeselect: true
});